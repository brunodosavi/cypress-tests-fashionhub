{
  "name": "fashionhub",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "e2e:chrome": "cypress run --browser chrome",
    "cy:prod": "cypress run --config-file cypress.production.config.js",
    "cy:staging": "cypress run --config-file cypress.staging.config.js",
    "cy:local": "cypress run --config-file cypress.local.config.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cypress": "^13.15.1",
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "cypress-dotenv": "^2.0.0"
  }
}